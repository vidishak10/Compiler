DATABASES = {
    "mysql": {
        "host": "localhost",  # Ensure MySQL is running on this host
        "user": "root@localhost",  # Check MySQL user
        "password": "Root",  # Verify password
        "database": "compiler"  # Ensure the database exists
    },
    "postgresql": {
        "host": "localhost",  
        "user": "your_user",
        "password": "your_password",
        "dbname": "your_database"
    }
}
